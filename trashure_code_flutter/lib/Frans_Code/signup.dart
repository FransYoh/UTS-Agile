import 'package:flutter/material.dart';

class Sign_Form extends StatelessWidget {
  const Sign_Form({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text("login gans")],
    );
  }
}
